# GCP
GCP_PROJECT_ID='your-gcp-project-id'   # your own GCP project ID
GCP_REGION='asia-southeast1'

# BigQuery
BQ_DATASET_RAW='dev_raw'                # raw landing dataset in your GCP project
DEV_SCHEMA_PREFIX='dev_yourhandle'      # dbt creates your staging/intermediate/marts/seed datasets

# LTA DataMall API
LTA_API_KEY='your_lta_api_key'

# OneMap — RESERVED, not yet used by any code (planned for population/demand work).
# Leave blank until a source actually reads these.
ONEMAP_EMAIL=''
ONEMAP_EMAIL_PASSWORD=''
